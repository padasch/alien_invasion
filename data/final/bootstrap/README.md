# Bootstrap summaries

This directory contains the compact, validated bootstrap summaries consumed by
the main and supplementary figure scripts. They are generated by the production
model scripts in `scripts/auxiliary/bootstrap/` using 1,000 successful
container-cluster refits.

Replicate-level draws, fitted model objects, and diagnostic graphics are not
tracked here. The local `exploration/` directory is optional scratch space and
is ignored by Git.
